<H1>Gift Lists</H1>

<ul>
  <% @giftlists.each do |giftlist| %>
   <li><%= link_to giftlist.name, giftlist_path(giftlist) %></li>
   <% end %>
<br>
   <%= link_to "new giftlist", new_giftlist_path %>
</ul>
